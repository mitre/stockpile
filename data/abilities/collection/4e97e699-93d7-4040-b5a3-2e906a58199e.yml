---

- id: 4e97e699-93d7-4040-b5a3-2e906a58199e
  name: Stage sensitive files
  description: copy files to staging directory
  tactic: collection
  technique:
    attack_id: T1074.001
    name: "Data Staged: Local Data Staging"
  platforms:
    darwin:
      sh:
        command: |
          cp "#{file.path[filters(technique=T1005,max=3)]}" #{file.dir[filters(max=1)]}
    linux:
      sh:
        command: |
          cp "#{file.path[filters(technique=T1005,max=3)]}" #{file.dir[filters(max=1)]}
    windows:
      psh:
        command: |
          Copy-Item #{file.path[filters(technique=T1005,max=3)]} #{file.dir[filters(max=1)]}
      cmd:
        command: |
          copy #{file.path[filters(technique=T1005,max=3)]} #{file.dir[filters(max=1)]}
  requirements:
    - plugins.stockpile.app.requirements.paw_provenance:
      - source: file.path
    - plugins.stockpile.app.requirements.paw_provenance:
      - source: file.dir
