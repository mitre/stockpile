- id: 3d060da6-ae05-475e-9a6f-871be489209a
  name: Shared Modules
  description: Ability for Shared Modules (ATT&CK T1129). Generated from Atomic Red
    Team procedures; review and test safely before use.
  tactic: execution
  technique:
    attack_id: T1129
    name: Shared Modules
  platforms:
    windows:
      psh:
        command: '#{pscp_file} -pw #{vm_pass} #{vib_file} #{vm_user}@#{vm_host}:/tmp
          echo "" | "#{plink_file}" "#{vm_host}" -ssh -l "#{vm_user}" -pw "#{vm_pass}"
          -m "#{vib_install}"'
